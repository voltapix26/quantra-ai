{
  "name": "quantra-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Quantra AI — Android shell (Capacitor wrap of the live PWA)",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1"
  }
}
